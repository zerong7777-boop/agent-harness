# Machines

`machines/` holds named remote resources.

Each machine record should capture:

- stable machine id
- host or alias
- platform
- GPU summary
- storage summary
- stable root paths
- current availability status
